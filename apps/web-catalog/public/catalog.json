{
  "generatedAt": "2026-06-12T17:12:56.766Z",
  "environment": "dev",
  "events": [
    {
      "id": "payment.created",
      "version": "1.0.0",
      "domain": "payments",
      "name": "Payment Created",
      "description": "Evento emitido cuando se crea un pago.",
      "producer": "payment-service",
      "consumers": [
        "billing-service",
        "notification-service"
      ],
      "tags": [
        "payment",
        "billing",
        "async"
      ],
      "payloadSchema": {
        "type": "object",
        "required": [
          "paymentId",
          "userId",
          "amount",
          "currency"
        ],
        "properties": {
          "paymentId": {
            "type": "string"
          },
          "userId": {
            "type": "string"
          },
          "amount": {
            "type": "number"
          },
          "currency": {
            "type": "string",
            "enum": [
              "USD",
              "ARS",
              "EUR"
            ]
          }
        }
      },
      "examples": [
        {
          "paymentId": "pay_123",
          "userId": "usr_456",
          "amount": 120.5,
          "currency": "USD"
        }
      ],
      "destination": {
        "provider": "aws.sns",
        "topicArnRef": "PAYMENTS_TOPIC_ARN",
        "regionRef": "AWS_REGION"
      },
      "usage": {
        "typescript": "await client.payments.paymentCreated.publish(payload);",
        "java": "client.payments().paymentCreated().publish(payload);",
        "python": "await client.payments.payment_created.publish(payload)",
        "go": "client.Payments().PaymentCreated().Publish(ctx, payload)"
      },
      "consumerUsage": {
        "typescript": "const consumer = createConsumer({ payments: { paymentCreated: async (payload, envelope) => { /* handle */ } } });\nawait consumer.handle(envelope);",
        "java": "EventConsumer consumer = EventConsumer.builder()\n    .payments()\n    .paymentCreated((payload, envelope) -> { /* handle */ })\n    .build();\nconsumer.handle(envelope);",
        "python": "consumer = create_consumer(payments={\"payment_created\": handle_payment_created})\nawait consumer.handle(envelope)",
        "go": "consumer := events.NewConsumer(events.ConsumerHandlers{\n    Payments: &events.PaymentsConsumerHandlers{\n        PaymentCreated: handlePaymentCreated,\n    },\n})\nhandled, err := consumer.Handle(ctx, envelope)"
      }
    }
  ]
}
